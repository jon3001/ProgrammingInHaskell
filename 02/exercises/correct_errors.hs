-- correct any syntax errors in the proposed code

n = a `div` length xs -- use lower case function name
    where -- correct indentation
        a = 10 
        xs = [1,2,3,4,5] -- correct indentation